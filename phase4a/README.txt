Testcase 20:

Our program produces the exact same output as the autograder. The only difference is the two lines are out of order
by one line, which comes from a race condition. Our output is still correct and gives the same information.